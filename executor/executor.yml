---
listen_address: ":8080"
verbose: true
commands:
  - cmd: curl
    args: ["-fsSX", "POST", "http://127.0.0.1:7000/on"]
    match_labels:
      alertname: HighTemperature
  - cmd: curl
    args: ["-fsSX", "POST", "http://127.0.0.1:7000/off"]
    match_labels:
      alertname: LowTemperature
